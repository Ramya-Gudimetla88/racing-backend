stages:
  - test
 
backend_ut_tests:
  stage: test
  image: python:3.10
  script:
    - pip install -r requirements.txt
    - coverage run manage.py test
    - coverage xml
  artifacts:
    reports:
      coverage_report:
        coverage_format: cobertura
        path: coverage.xml